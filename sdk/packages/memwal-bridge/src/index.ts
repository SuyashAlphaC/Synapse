/**
 * @synapse-core/memwal-bridge — wraps MemWal SDK with AgentIdentity context.
 * Phase 2 implementation per the plan; bindings land here as the real API
 * surface stabilizes against MystenLabs/MemWal.
 */

export const MEMWAL_BRIDGE_VERSION = '0.1.0';
