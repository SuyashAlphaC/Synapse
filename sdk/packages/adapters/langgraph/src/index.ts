/**
 * @synapse-core/adapter-langgraph — Synapse as a LangGraph memory + identity
 * backend. Implements BaseStore and BaseCheckpointSaver interfaces. Phase 2.
 */

export const LANGGRAPH_ADAPTER_VERSION = '0.1.0';
