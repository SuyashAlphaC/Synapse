/**
 * @synapse-core/adapter-claude-sdk — Synapse for Claude Agent SDK.
 * Implements memory hooks + tool authorization so every Claude tool call
 * routes through `wallet::spend` with policy gating. Phase 2.
 */

export const CLAUDE_SDK_ADAPTER_VERSION = '0.1.0';
